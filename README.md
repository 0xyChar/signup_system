A full-stack authentication system built with Django REST Framework and Vue.js that handles user registration, login, email verification, password reset and a personalized dashboard that displays user data.The backend uses Django with PostgreSQL for data storage and JWT for authentication,
while the frontend is built with Vue.js.
