A full-stack authentication system built with Django REST Framework and Vue.js that handles user registration, login, email verification, password reset, and a personalized dashboard. Users sign up with email and username, receive verification emails, and access a secure dashboard displaying real-time user statistics and recent signups. The backend uses Django with PostgreSQL for data storage and JWT for authentication,
while the frontend is built with Vue.js with a clean, responsive UI.
